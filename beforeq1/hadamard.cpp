#include<iostream>
#include<vector>
using namespace std;
void hadamard(vector<int>&x,int start,int stop,int line,vector<int>&tmp){
    int m = (start+stop)/2;
    if(start==stop){

    }

    hadamard(x,num+1,final,tmp);
}
int main(){
    int x,y;
    cin>>x;
    vector<int> vec(x);
    vector<int> tmp(x,1);
    for(int i = 0;i<x;i++){
        cin >> y;
        vec[i] = y;
    }
}